set-option -sa terminal-overrides ",xterm*:Tc"
set -g mouse on
set -g default-terminal "tmux-256color"

unbind C-b
set -g prefix C-Space
bind C-Space send-prefix

# Vim style pane selection
bind h select-pane -L
bind j select-pane -D 
bind k select-pane -U
bind l select-pane -R

# Start windows and panes at 1, not 0
set -g base-index 1
set -g pane-base-index 1
set-window-option -g pane-base-index 1
set-window-option -g window-status-format "#[fg=#908caa]#I  #W"
set-window-option -g window-status-current-format "#[fg=#f6c177,bold]#I  #W"
set -g window-status-separator "  "

set-option -g renumber-windows on
set-option -g status-justify centre
set-option -g status-position bottom
set-option -g status-bg default
set-option -g status-fg "#908caa"
set-option -g status-style "bg=default,fg=#908caa"

# Left section (session and window)
set -g status-left-length 100
set -g status-left "#[fg=#c4a7e7,bold] #S #[fg=#6e6a86,nobold] #[fg=#f6c177]#I #[fg=#e0def4] #W"

# Right section (user, time, host, directory)
set -g status-right-length 150
set -g status-right "#[fg=#9ccfd8] #(whoami) #[fg=#6e6a86] #[fg=#f6c177]%H:%M #[fg=#6e6a86] #[fg=#c4a7e7]#H #[fg=#6e6a86] #[fg=#ebbcba] #(basename #{pane_current_path})"


# Alt-arrow keys to switch panes without prefix
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

# Shift arrow to switch windows
bind -n S-Left  previous-window
bind -n S-Right next-window

# Shift Alt vim keys to switch windows
bind -n M-H previous-window
bind -n M-L next-window

# TPM plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'tmux-plugins/tmux-resurrect'

run '~/.tmux/plugins/tpm/tpm'

# set vi-mode
set-window-option -g mode-keys vi

# keybindings
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi C-v send-keys -X rectangle-toggle
bind-key -T copy-mode-vi y send-keys -X copy-selection-and-cancel

bind '"' split-window -v -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}"
